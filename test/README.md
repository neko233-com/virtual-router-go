# test 目录说明

本目录用于统一管理项目的单元测试与测试辅助代码。

## 约定
- 测试目录按功能域拆分，例如：`test/virtual_router_client/`。
- 测试函数命名优先体现业务场景，例如：`TestClient_冲突后不崩溃且可自动重连`。
- 关键断言前请补充简短中文注释，说明“场景/预期/为什么”。
- 异步场景统一使用小步轮询 + 超时，避免不稳定的 `Sleep` 常量堆叠。

## 运行
```powershell
# 在仓库根目录执行
go test ./...
```

